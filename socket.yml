version: 2
issueRules:
  # Redis server-side Lua scripting (EVAL/EVALSHA via the redis/ioredis
  # clients) in packages/ratelimit/core/src/store/redis.ts is flagged as
  # dynamic code execution. It is NOT JavaScript eval(). Reviewed false
  # positive - see SECURITY.md.
  usesEval: false
  # Only NODE_ENV and NEXT_PHASE are read (packages/ratelimit/core/src/env.ts)
  # for adapter selection and Next.js build-phase detection. No credentials.
  # Reviewed false positive - see SECURITY.md.
  envVars: false
