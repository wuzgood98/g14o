---
title: Execution
description: Parallel and sequential listener strategies.
---

```ts
import { Event, sequentialStrategy } from "@g14o/events";
import { memoryStream } from "@g14o/events/memory";

const event = new Event<Events>({
  stream: memoryStream(),
  strategy: sequentialStrategy,
});
```

Default is `parallelStrategy`.
