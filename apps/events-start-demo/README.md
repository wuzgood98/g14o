# Events TanStack Start demo

Demonstrates `@g14o/events` with TanStack Start server routes and React client hooks on one origin.

## Run

```bash
pnpm demo:events-start
```

Open http://localhost:3030

## Mount

See [Handler — TanStack Start](/packages/events/handler#tanstack-start) and `src/routes/api/events.ts`.
